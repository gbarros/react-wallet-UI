{
  "name": "react-wallet-panel-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=demo",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "clean": "npm run clean --workspaces"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "viem": "^2.37.2",
    "wagmi": "^2.16.9"
  },
  "overrides": {
    "@privy-io/react-auth": "^2.24.0",
    "permissionless": "^0.2.56",
    "ox": "^0.9.3",
    "vite": "^5.4.0",
    "vite-plugin-dts": "^4.0.0",
    "esbuild": "^0.21.0"
  }
}
